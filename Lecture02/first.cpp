#include<iostream>
using namespace std;

int main () {

    // int a, b;
    // cout << "Enter your first number:";
    // cin >> a;
    // cout << "Enter your second number:";
    // cin >> b;

    // cout << "Your sum is: " << a + b;

    // int c = 'a' + 1;
    // cout << c;

    // Arithmetic operator :
    // + - *  / % 
    // int a = 20;
    // int b = 10;

    // cout << a + b << endl;
    // cout << a - b << endl;
    // cout << a * b << endl;
    // cout << a / b << endl;
    // cout << a % b << endl;
       
    // Assignment operator:

    // a += b;
    // a = a + b;

    // a -= b;
    // a = a - b; 

    // a *= b;
    // a = a * b; 

    // a /= b;
    // a = a / b; 

    // cout << a;

    // Comparison operator:
    // >, <, >=, <=, !=, 
    // int a = 20;
    // int b = 10;

    // cout << (a>b);
    // cout << (a<b);
    // cout << (a>=b);
    // cout << (a<=b);
    // cout << (a!=b);

    // int a = 20;

    // Left shift 
    // a = a << 3;

    // Right shift
    // a = a >> 3;

    // cout << a;

    // Logical operator:
    // && (and) : Both should be true --> Then only true, else false.
    // || (or) : If both side is false --> then only false, else true.
    //  ! (Not operator) : Reverse.

    // cout << (true && true);
    // cout << (true && false);
    // cout << (true || true);
    // cout << (true || false);
    // cout << (false || false);
    // cout << (!true);
    // cout << (!false);

    // Increment/ Decrement operator:

    // int a = 10;
    
    // pre increment , post increment
    // pre decrement , post decrement

    // int b = a++;
    //int b = ++a;
    // int b = a--;
    // int b = --a;

    // cout << b << endl;
    // cout << a << endl;

    // Bitwise operator:
    // & , | , ^

    int a = 10;
    int b = 5;

    cout << (a & b) << endl;
    cout << (a | b) << endl;
    cout << (a ^ b) << endl;

    return 0;
}